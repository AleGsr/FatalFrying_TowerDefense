using UnityEngine;

public class Sauce : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Shoot()
    {
        //Dispara salsa que se queda por corto tiempo en el suelo haciendo un daño masivo
    }

    void UpgradeSauce()
    {
        //De acuerdo al tiempo que va pasando o de manera random:
        //Se va ir mejorando la salsa de manera que se va a aumentar el daño, el rango de los enemigos afectados
        //y el tiempo que se queda la salsa en el piso causando daño.
    }

}
